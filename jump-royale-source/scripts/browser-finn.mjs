import assert from 'node:assert/strict';import {createRequire} from 'node:module';const require=createRequire(import.meta.url),{chromium}=require('C:/Users/Swagg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const b=await chromium.launch({headless:true,channel:'msedge'});try{const p=await b.newPage({viewport:{width:1440,height:900}});await p.goto('http://127.0.0.1:5245');await p.waitForFunction(()=>window.__FORGE_DEV__?.scene.isActive('Menu'));await p.evaluate(()=>localStorage.setItem('jump-royale-wallet-v1',JSON.stringify({gold:20,owned:[],rewards:[]})));await p.getByRole('button',{name:'Open wardrobe',exact:true}).click();await p.getByRole('button',{name:'Finn · 16-bit',exact:true}).click();await p.getByRole('button',{name:'Finn · 16-bit',exact:true}).click();await p.getByRole('button',{name:'Close wardrobe',exact:true}).click();await p.reload();await p.waitForFunction(()=>window.__FORGE_DEV__?.scene.isActive('Menu'));assert.equal(await p.evaluate(()=>window.__FORGE_DEV__.scene.getScene('Menu').preview.texture.key),'fantasy-finn-16');await p.screenshot({path:'../qa-finn-hd.png'});await p.keyboard.press('d');await p.keyboard.down('Space');await p.waitForTimeout(350);await p.keyboard.up('Space');await p.waitForTimeout(100);assert.equal(await p.evaluate(()=>window.__FORGE_DEV__.scene.getScene('Menu').preview.texture.key),'fantasy-finn-16');await p.getByRole('button',{name:'Start',exact:true}).click();await p.waitForTimeout(1500);await p.screenshot({path:'../qa-finn-match.png'});console.log('PASS Finn HD purchased through wardrobe, equipped, persisted on reload, lobby jump and match start');}finally{await b.close();}
