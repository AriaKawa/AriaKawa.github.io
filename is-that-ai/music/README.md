# Is That AI? music pack

Place the ten final audio files in this folder as `round-01.mp3` through `round-10.mp3`.
Then update the matching title, artist, and `ai`/`human` answer in `../tracks.js`.

Until those files arrive, the interface uses a clearly labeled silent timing demo so the full multiplayer game can still be tested.
