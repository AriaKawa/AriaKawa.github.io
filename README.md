# AriaKawa.github.io
## Smash icon cache

Local character icons for `Smash.html` live in `assets/smash/icons`. Populate or refresh them by running:

```bash
python3 scripts/fetch-smash-icons.py
```

Re-run the script whenever the Smash Ultimate character list changes so the local cache stays current.
