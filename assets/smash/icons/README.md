# Smash icons (local cache)

Run the download script to populate this folder with the latest character icons:

```bash
python3 scripts/fetch-smash-icons.py
```

Re-run the script any time the Smash Ultimate API character list changes so the local cache stays in sync.
